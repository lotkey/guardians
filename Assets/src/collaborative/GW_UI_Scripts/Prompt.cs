using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Prompt : HUDElement
{
    // Start is called before the first frame update
    void Start()
    {
       this.SetActive(activeDefault); 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
